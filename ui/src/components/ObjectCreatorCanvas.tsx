import styles from "./ObjectCreatorCanvas.module.scss";

const ObjectCreatorCanvas = () => {
  return (
    <div class={styles.container}>
      Object Creator canvas!
    </div>
  );
}

export default ObjectCreatorCanvas;